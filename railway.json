{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && corepack enable && corepack prepare npm@latest --activate && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && npm start"
  }
}